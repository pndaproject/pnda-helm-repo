# Default values for platform-testing.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: containers.cisco.com/donaldh/platform-testing
  tag: 0.1.1
  pullPolicy: IfNotPresent

